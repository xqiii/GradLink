{
  "name": "link-map-backend",
  "version": "1.0.0",
  "description": "中国地图数据可视化应用后端API",
  "main": "src/server.js",
  "scripts": {
    "start": "npm run build && node dist/server.js",
    "dev": "ts-node-dev --transpile-only --respawn src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start:ts": "node dist/server.js",
    "dev:ts": "ts-node-dev --transpile-only --respawn src/server.ts",
    "test": "jest"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
